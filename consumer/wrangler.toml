# `name` is injected by .github/workflows/deploy.yml at deploy time
# (e.g. wildebeest-consumer-${NAME_SUFFIX}). Local dev: pass --name explicitly.
compatibility_date = "2026-04-25"
compatibility_flags = ["nodejs_compat"]
main = "./src/index.ts"
